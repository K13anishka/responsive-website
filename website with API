<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>India: Country Profile</title>
    <style>
        body {
            background-color: #eef6fa;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            color: #3e2723;
            transition: background-color 0.5s ease;
        }

        .container {
            max-width: 700px;
            margin: 40px auto;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            padding: 30px;
            text-align: center;
        }

        .flag {
            display: block;
            width: 240px;
            height: 144px;
            margin: 0 auto 20px auto;
            border: 2px solid #222;
            border-radius: 6px;
        }

        h1 {
            color: #0057a4;
            font-size: 2.6em;
            background-color: #f3e5ab;
            margin-bottom: 15px;
            border-radius: 8px;
            padding: 10px 0;
        }

        .info p {
            font-size: 1.2em;
            background-color: #ffe0b2;
            padding: 10px;
            border-radius: 6px;
            margin: 8px 0;
            text-align: left;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .info p span {
            color: #1565c0;
            font-weight: bold;
        }

        .brief-info {
            background-color: #f9fbe7;
            color: #444;
            border-left: 7px solid #ff9800;
            padding: 12px;
            margin-top: 15px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
            text-align: left;
        }

        .footer {
            margin-top: 40px;
            color: #888;
            font-size: 1em;
        }

        button {
            background-color: #0057a4;
            color: white;
            border: none;
            padding: 12px 24px;
            font-size: 1em;
            border-radius: 6px;
            cursor: pointer;
            margin-top: 25px;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #003f7d;
        }

        .fetched-data {
            margin-top: 30px;
            background: #e0f7fa;
            border-radius: 9px;
            padding: 18px;
            text-align: left;
            display: none; /* Hidden until data fetched */
        }
        .fetched-data h2 {
            font-size: 1.3em;
            color: #0057a4;
            margin-top: 0;
        }
        .fetched-data ul {
            list-style-type: disc;
            margin-left: 24px;
            color: #444;
            padding-left: 10px;
        }

        @media screen and (max-width: 767px) {
            .container {
                max-width: 90%;
                padding: 20px;
            }
            .flag {
                width: 180px;
                height: 108px;
            }
            h1 {
                font-size: 2em;
                padding: 8px 0;
            }
            .info p { font-size: 1em; padding: 8px; }
            .brief-info { font-size: 0.9em; padding: 10px; }
        }

        @media screen and (max-width: 480px) {
            .flag { width: 140px; height: 84px; }
            h1 { font-size: 1.6em; }
            .info p { font-size: 0.9em; }
            .brief-info { font-size: 0.85em; }
        }
    </style>
</head>
<body>

    <div class="container">
        
        <img
            src="https://en.wikipedia.org/wiki/Flag_of_India#/media/File:Flag_of_India.svg"
            alt="Flag of India"
            class="flag"
        >
        
        <h1>India</h1>

        <div class="info">
            <p><b>Capital:</b> <span>New Delhi</span></p>
            <p><b>Official languages:</b> <span>Hindi, English</span></p>
            <p><b>Population:</b> <span>Over 1.4 billion</span></p>
            <p><b>Area:</b> <span>3,287,263 km²</span></p>
            <p><b>Currency:</b> <span>Indian Rupee (INR)</span></p>
            <p><b>Independence day:</b> <span>August 15, 1947</span></p>
            <p class="brief-info">
                India is located in South Asia and is the world's largest democracy. Known for its rich heritage and vibrant culture, India is home to many important world sites including the Taj Mahal. The country celebrates a variety of colorful festivals and is the birthplace of religions like Hinduism, Buddhism, Jainism, and Sikhism.
            </p>
        </div>

        <!-- Button to change background color -->
        <button id="colorButton">Change Background Color</button>

        <!-- Button to fetch user data -->
        <button id="fetchDataButton" style="margin-left:10px;background-color:#ff9800;">Fetch Sample User Data</button>

        <!-- Container to show the fetched data -->
        <div class="fetched-data" id="fetchedData">
            <h2>Sample Users</h2>
            <ul id="userList">
                <!-- Dynamically populated -->
            </ul>
        </div>

        <div class="footer">
            &copy; 2025 India Country Profile
        </div>
    </div>

    <script>
        // Background color changer
        const colorButton = document.getElementById('colorButton');
        const colors = ['#eef6fa', '#ffe0b2', '#f9fbe7', '#d0f0fd', '#ffe4e1', '#e0f7fa'];
        let currentIndex = 0;

        colorButton.addEventListener('click', () => {
            currentIndex = (currentIndex + 1) % colors.length;
            document.body.style.backgroundColor = colors[currentIndex];
        });

        // Fetch API example: JSONPlaceholder users
        const fetchDataButton = document.getElementById('fetchDataButton');
        const fetchedDataDiv = document.getElementById('fetchedData');
        const userList = document.getElementById('userList');

        fetchDataButton.addEventListener('click', () => {
            fetchDataButton.disabled = true;
            fetchDataButton.textContent = "Loading...";
            fetch('https://jsonplaceholder.typicode.com/users')
                .then(response => response.json())
                .then(data => {
                    userList.innerHTML = ''; // Clear previous list if any
                    data.slice(0, 5).forEach(user => {
                        const li = document.createElement('li');
                        li.textContent = `${user.name} (${user.email})`;
                        userList.appendChild(li);
                    });
                    fetchedDataDiv.style.display = 'block';
                })
                .catch(error => {
                    userList.innerHTML = '<li style="color:red;">Failed to fetch data</li>';
                    fetchedDataDiv.style.display = 'block';
                })
                .finally(() => {
                    fetchDataButton.disabled = false;
                    fetchDataButton.textContent = "Fetch Sample User Data";
                });
        });
    </script>

</body>
</html>
